package io.castled.android.notifications.workmanager.models

enum class CastledNetworkRequestType {

    PUSH_REGISTER, PUSH_EVENT, IN_APP_EVENT, TRACK_EVENT, INBOX_EVENT, USER_TRACKING, DEVICE_INFO,
    LOGOUT, SESSION
}